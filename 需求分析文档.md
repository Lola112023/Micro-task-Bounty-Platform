## 一、项目背景

在当代大学校园环境中，学生兼职需求与零散技能服务（代取快递、修图、文档排版、设计、编程小任务等）日益增多。然而，现有的任务对接渠道主要依赖微信群、朋友圈、校园论坛等非正式平台，存在以下问题：信息零散易错过、缺乏信用评价体系导致交易风险高、任务进度无法跟踪、纠纷难以处理、报酬支付缺乏保障。

与此同时，许多学生希望通过完成小型任务赚取报酬，或有零散技能（设计、编程、文案）希望变现，但缺乏一个安全可靠、有信用背书、流程规范的校内任务对接平台。

本项目面向本校大学生群体，打造集任务发布、接单、交付、结算、信用评价于一体的任务悬赏平台，帮助学生高效发布和完成小型任务，规范交易流程、保障双方权益、提升任务完成效率，具有较强的实用价值与社会意义。

## 二、项目目标

1. 实现北京理工大学大学校内统一身份认证，确保仅北京理工大学师生可使用平台，建立安全可信的用户体系。
2. 支持任务发布、浏览、搜索、接单、中标选择等完整任务流转能力。
3. 实现任务状态自动化流转（发布中 → 进行中 → 待确认 → 已完成/已取消，以及申诉中），各环节时间节点可追溯。
4. 建立信用评价体系，任务完成后双方互评，累计信用分，作为后续接单/发布的参考依据。
5. 提供个人收支统计与任务记录，支持用户查看累计收入/支出、任务完成率、历史交易明细。

## 三、核心功能

### 1、 用户认证与个人中心

#### 1.1 校内身份认证

- 仅北京理工大学师生可使用，注册时需填写姓名，学号，书院/学院等基本信息
- 账户与学号/工号一一对应，禁止重复注册。

#### 1.2 个人中心的信息管理

用户可在个人中心界面设置头像（必有）、昵称（必有、唯一）、公告栏（可选），查看信用分、完成率、累计收入/支出等统计数据。

- **头像设置**
  - 默认头像为系统内置的通用头像图片。
  - 用户可自行上传头像，要求尺寸严格为225×225像素，文件大小<1MB，仅支持jpg/png格式。
  - 提交头像修改申请后，系统自动进入待审核状态，管理员在后台进行审核，24小时内必须完成审核并反馈结果。审核不通过需附带简短原因，用户头像恢复为申请前头像；超时未审视为自动拒绝并通知管理员。（关于自动拒绝的说明，见附录6）同一用户对同一类型（头像，昵称等）只能有一个“待审核”的申请，审核期间无法再次提交。若用户再次提交，系统提示“已有申请正在处理，请耐心等待”。
  - 审核期间，用户头像保持原图。
- **昵称设置**
  - 仅可使用大小写英文字母、中文字符、阿拉伯数字，不可包含空格及其他特殊字符。对名称中含有的字符种类数不做限制。但必须小于10个字（关于“字”的定义见附录1，后续不做赘述）。
  - 昵称全局唯一，区分大小写，系统在注册时自动生成默认昵称（格式：“用户” + 8位随机字母数字组合），保证不与他人重复。
  - 用户可提交昵称修改申请，提交后该昵称立即进入“锁定”状态，其他用户在此期间不可申请相同昵称。
  - 管理员须在24小时内审核，通过后昵称立即生效；拒绝则继续保持原昵称，并释放该昵称的锁定状态。若24小时未处理，则系统自动判定为申请失败并继续使用原先的昵称。（此种情况并不占用每30天一次的修改机会）并通知管理员有超时未处理的请求。此超时请求无法恢复，仅在管理员审核界面显示“超时未处理”。后续是否重新处理，取决于用户是否重新提交申请。
  - 每个用户每30天仅可提交一次昵称修改申请。
- **公告栏设置**
  - 为一段最长200字的纯文本字符串，用户可填写联系方式、个人简介等补充信息。
  - 修改公告栏同样需提交申请，管理员在24小时内审核，审核不通过则回滚至修改前内容。超时未审视为自动拒绝并通知管理员。
- **信用分**
  - 信用分采用百分制，初始信用分为80分。
  - 影响因素及默认算法（管理员可在后台调整权重系数）：
    - **历史完成率**：已完成任务数 / 已承接任务总数（不含取消）。完成率≥90% +5分，70%~90% 不加分，<70% -5分，更新时机为每次任务终结后，在当天24:00重算。下面给个例子说明这个问题：
      - 用户当前完成率95%（已加5分），完成一个新任务后完成率降为85%。此时：根据新完成率85%（属于70%~90%）不加分，但之前加的5分予以保留。也即那些完成度不高的，必须在一定时间内大量地完成订单来弥补，不然信用分会慢慢地被扣完。
    - **好评率**：获得4星及以上的评价数 / 收到的评价总数。好评率≥95% +3分，80%~95% 不加分，<80% -3分。
    - **超时次数**：每发生一次接单后超时未交付且被发布者强制取消，扣8分；接单者主动放弃任务（需双方协商一致后由接单者取消）每次扣3分。如果接单者超时后主动与发布者协商放弃，发布者同意取消（而非强制取消），此时叠加扣分。
    - **主动取消任务（发布者）**：发布者发布后无故下架任务（非无人接单自动取消），每次扣5分。
    - 信用分下限为0分，上限为100分。分数低于60分时，系统将在发布/接单时给予风险提示；低于40分将被限制接单（只可发布任务），但依旧可以完成限制接单之前已接到的订单。低于40分之后，账号生命周期内可向管理员提出恢复信用分申请一次，并附以500字以内的申请陈述。管理员需在24小时内根据陈述申请做出拒绝/同意的决定，并附以50字以内的说明性文字。管理员24小时内不回复，则默认通过申请。
  - 信用分在个人中心实时可见，详细加减分记录可查看“信用分明细”，明细页面每页15条记录，显示：完成时间，任务类型，获评的发布者星级与接单者星级、发布者评价四种信息。

#### 1.3 角色切换

- 个人中心顶部提供“发布者视角”与“接单者视角”切换标签。
- 发布者视角下默认展示“我发布的任务”列表；接单者视角下展示“可接任务推荐”及“我的申请”入口。切换视角仅改变快捷入口与首页默认模块，不丢失另一身份的数据。

### 2、 任务发布与管理

#### 2.1 发布任务

- 发布者填写任务信息：
  - **任务名称**：必填，最多30字。
  - **详细描述**：必填，仅支持纯文本，最长2000字。
  - **任务分类**：必填，下拉选择，选项来源于管理员维护的动态分类标签（如跑腿、设计、编程、文案、其他），分类标签只能选一个。
  - **校区**：必填，仅支持良乡校区、中关村校区、两校区往返，三种选项。以下拉选择的形式呈现。
  - **截止时间**：必填，指任务发布时设定一个时长（分钟），从中标那一刻开始倒计时。大于等于30分钟，小于等于43200分钟（即30天）。
  - **预估报酬**：正整数，单位为“积分”（积分的详细信息见“6、积分系统”），范围1-5000积分。发布时从发布者账户立即冻结对应积分，任务完成后划转；若取消则退回。
  - **附件**：可选，支持上传zip/rar/图片/文档，单个大小不超过20MB，最多3个文件，总大小不超过30MB。（任务完成后30天清理发布附件）
- 提交后任务经过简单的敏感词过滤（如脏话、非法交易等）即立即进入“发布中”状态（关于过滤机制，见附录7），出现在任务列表。其余用户可举报此任务存在未被检测出的不当行为（由管理员根据公序良俗主观判断），如果举报核实，划扣100%发布的任务的积分，归入平台收益账户（见6.3节）。并根据情节严重性对涉事账号进行5天至永久的冻结处理，以及扣除部分信誉分。（关于举报，详细见“2.5 举报功能”）
- 发布的任务在任务列表存在的时间为N天（N由用户自行选择，其值大于等于1，小于等于14），N天之后还没有进入执行阶段的订单，此任务会直接被取消。发布者的积分将回到自己的账户，不做任何扣除。这里的不进入执行阶段既可以指无人接单，也可以指发布者没有找到合适的接单者。

#### 2.2 编辑与下架

- 仅当任务处于“发布中”状态（无人接单）时，发布者可编辑任务名称、描述、分类、截止时间、报酬及附件。修改报酬需差额冻结/解冻积分。当有人竞标时，任务内容不可做任何更改。
- 发布者可主动下架任务（状态变为“已取消”），冻结积分由系统划扣5%以示惩罚，其余退回，并且记录操作日志。主动下架次数过多将影响信用分（见1.2信用分规则）。

#### 2.3 我的任务列表

- 以列表形式展示发布者所有任务，每项展示：名称、分类、报酬、发布时间、当前状态。默认按照发布时间由近到远排列，发布时间相同的，按照“发布中、进行中、待确认、已完成、已取消”的顺序排列，再相同的，随机排列。
- 状态包含：发布中、进行中、待确认、已完成、已取消。（详细定义见**“4.1 任务状态机”**）支持按状态筛选。如果以状态筛选，则以“发布中、进行中、待确认、已完成、已取消”的顺序呈现，相同状态内部以发布时间由近到远排序，都相同的，就随机排列。

#### 2.4 选择中标者

- 任务发布后，若有接单者申请，发布者在任务详情页看到“申请列表”。
- 申请列表展示：申请者昵称、信用分、简介、历史完成率、申请时间。默认按信用分降序排列。
- 发布者可在其中选择一人作为中标者，点击确认后任务状态变为“进行中”，并占用接单名额（同时接单上限为3个任务）。未被选中的申请者状态自动变为“已落选”，并收到系统通知。接单额度直到任务进入“已完成”状态后才释放。
- 关于任务的取消，见**“2.1 发布任务”**。

#### 2.5 举报功能

- 在任务详情页和用户主页增加“举报”按钮。分别用以举报任务与用户。
- 举报流程：用户提交举报，选择举报类型、上传证据→ 系统通知管理员 → 管理员通过/驳回举报→ 系统通知双方结果 → 被举报方对处罚结果有异议，可提交“申诉”。
  - 关于举报类型：共设置四类，涉黄，涉暴，诈骗，其他。形式为下拉菜单选择。
  - 上传证据仅限文字，100字以内。
- 管理员可依据公序良俗、平台规定判断是否违规，如确实存在违规，则依据情节严重情况扣除被举报者1-10分信用分。如果是对任务的举报，则下架任务，并冻结账号5天。如果是对用户主页的举报，则清除主页内违规内容，并冻结账号10天。
- 管理员如果查明举报不实，举报者扣除3分信用分。

### 2.6 接单者、发布者交流功能

- 当订单进入“进行中”时，在任务交付界面会出现一个留言板，在留言板上接单者与发布者可以异步交流。
- 每条发送的消息都显示：发送者，时间（精确到分）
- 留言板在订单状态为“进行中”、“待确认”、“申诉中”状态均可用，直到任务进入“已交付”或“已取消”阶段。届时聊天记录会在后台保存一个月，过期自动清除。
- 留言板仅支持纯文本交流，每条消息不得多于50字，不限制条数。由于审核难度较大，不可发布图片或文档。文本发送前需经过系统敏感词的初步过滤。
- 申诉/举报发生时，留言板内的内容管理员可见，并可作为判罚的依据。
- 为避免刷屏，每分钟最多发送5条消息。

### 3、 任务浏览与接单

#### 3.1 任务列表浏览

- 所有已发布且未被接取的任务出现在公共任务大厅。
- 对于发布中（未中标）的任务，显示“待中标后计时”或显示“任务下架时间”（发布后14天）
- 筛选：可按任务分类（多选）、报酬范围（最低-最高）、发布时间段（最远到14天前）筛选。
- 搜索：支持任务名称关键词模糊搜索。
- 排序：默认按剩余接单时间倒序，可切换为按报酬从高到低排序。
- 列表每项显示：任务名称、分类、报酬积分、剩余接单时间、发布者昵称（隐藏学号等隐私信息）。

#### 3.2 任务详情

- 点击卡片进入详情页，展示：任务名称、详细描述、分类、报酬、截止时间、附件下载链接、发布者昵称及其信用分。
- 详情页底部有“申请接单”按钮（需处于接单者视角，且信用分不低于40，同时未超出同时接单数上限3个，如果有一条不满足，这个按钮就是灰色的，没办法按）。

#### 3.3 申请接单

- 点击“申请接单”弹出申请框，必填申请理由（最短10字，最长200字），简要说明自身技能或经验。
- 提交后该申请出现在发布者的申请列表中，申请状态为“审核中”。
- 同一用户对同一任务不可重复申请。并且用户不可以申请自己发布的订单。

#### 3.4 我的申请记录

- 列表展示我申请过的所有任务，包含：任务名称、报酬、申请时间、申请状态（审核中/已中标/已落选/已取消-任务失效）。仅呈现这些信息，没有其他任何细节。
- 支持按状态筛选。排序同上。

### 4、 交付确认与状态流转

#### 4.1 任务状态机

- 严格遵循以下状态流转：
  - **发布中** — 发布者选择中标者 → **进行中**
  - **发布中** — 超时无人接单或发布者主动下架 → **已取消**
  - **进行中** — 接单者提交交付物 → **待确认**
  - **进行中** — 接单者超时未交付，发布者强制取消 → **已取消**
  - **进行中** — 接单者申请放弃，发布者在时间限制内同意申请 → **已取消**
  - **进行中** — 接单者申请放弃，发布者拒绝/无回应申请 → **进行中**
  - **待确认** — 发布者确认完成 → **已完成**
  - **待确认** — 发布者退回修改 → **进行中**
  - **待确认、进行中** — 发布者或中标者提出异议/发起申诉 → **申诉中**
  - **申诉中** — 管理员介入，可扭转至 **已取消** 或 **已完成** 或 **进行中**

#### 4.2 超时处理

- **无人接单超时**：任务发布后超过14天（默认，可配置）仍无人申请，系统自动将任务标记为“已取消”，报酬积分退还给发布者，并发送消息通知发布者。
- **交付超时**：中标后，系统以任务截止时间为准，若接单者在截止时间后仍未提交交付物，发布者在任务详情页可点击“强制取消”。系统会向接单者发送提醒：提醒时间取 min(24小时, 截止时长×50%)，且至少提前5分钟，超时后发布者操作前需二次确认，取消后任务变为“已取消”，冻结积分退回发布者，接单者记录一次超时扣信用分。
- 如因不可抗力需延期，双方可在截止时间前协商（关于协商的途径，见附录3），由发布者在任务页面操作“延长截止时间”（每次最多基于当前剩余时长延长50%，且总延长时间不超过原始时长的2倍。次数不超过2次），延期后超时判定按新时间。

#### 4.3 交付物上传

- 接单者在“进行中”任务里进入交付页面，可输入交付说明文本（最长1000字），上传交付附件（格式数量同任务附件，总大小≤30MB）。点击“提交交付”后任务状态变为“待确认”。“待确认”状态下，接单者本次的接单次数仍在被占用，仅当订单进入“已完成”阶段，才释放此次接单次数。

#### 4.4 确认完成

- 发布者在任务详情查看交付物，可选择“确认完成”或“退回修改”。如果三天内发布者未选择上述二者之中的一个，则自动将任务状态转为“已完成”，后续流程同“确认完成”。
- 若确认完成：任务状态变为“已完成”，冻结的报酬积分立即从发布者账户扣除并转入接单者账户。双方将收到一个通知，其中包含对应任务的评价入口，入口保留30日，逾期则此评价的星级将自动设置为双方当前的（逾期的时间点）评价星级，也即不会对双方的评价星级造成影响。确认完成之后，发布者不可再将订单状态逆转回“进行中”、“待确认”或是“申诉中”。故在发布者确认完成时，需弹出警告，确认已完成。
- 若退回修改：接单者需重新提交。若第3次提交仍被退回，发布者可发起申诉或申请管理员裁定，此时，接单者也可以发起申诉，管理员将根据主观判断进行积分的最终归属（全部退回发布者或划扣到接单者账户，没有其他可能）。

#### 4.5 申诉入口

- 在任务处于“待确认”或“进行中”超时争议时，任意一方可点击“申诉”提交理由和证据（文字+截图附件）。
- 管理员在后台任务申诉列表中查看，进行裁定，可强制扭转任务状态为“已完成”（并划转积分）或“已取消”（退款）或 “进行中”（无效申诉，继续完成任务），并填写处理意见。处理日志永久留存，相关人员信用分按裁定结果进行扣除或保持不变。
- 处于申诉中的订单，状态变更为“申诉中”。
- 关于申诉处理期间的时长计算：视情况而定（下面具体说明）。管理员会在24h内对申诉内容做出答复。
  - 如果状态流为“进行中”----“申诉中”----“进行中”，则申诉期间也算任务执行时间。申诉结束后不返还相应时间。
  - 其余情况，均返还申诉期的时间，也即相当于在申诉处理期间，冻结剩余执行时间。

#### 4.6 其余情况

- 在任务开始时，中标者可以向发布者申请取消订单。并附以200字以内的申请理由，同步通知申请者。如果申请者在min{24小时，任务剩余时长}内同意，则订单取消，并扣除中标者3分信用分。返还发布者全部积分。如果没有处理或选择了拒绝，订单依旧继续。

### 5、 信用评价与收支记录

#### 5.1 双方互评

- 任务完成后的24小时内，双方可在任务详情中进行评价，超时未评视为默认好评（5星，无文字）。
- 评价结构：星级（1-5星整数）+ 文字评论（可选，最长500字）。
- 评价内容展示在对方个人主页的评价列表中，按时间倒序，可被其他用户在任务详情和用户主页查看。

#### 5.2 信用分计算

- 已在上文1.2信用分中详细定义，管理员可在后台配置各项加减分值及阈值，后台修改后历史分数不变，新分数规则应用于后续变动。

#### 5.3 个人收支统计

- 个人中心汇总面板显示：
  - 当前积分余额（实时、仅自己可见）
  - 累计收入积分（仅自己可见）
  - 累计支出积分（仅自己可见）
  - 任务完成率：（已完成的承接任务数/承接任务总数）×100%，若无承接任务显示“N/A”。（所有人可见）
  - 信用分（所有人可见）
- 收入/支出明细列表：每一项记录包括时间、关联任务名称、积分变化额、类型（收入/支出/冻结/解冻），支持分页和按时间段筛选。（仅自己可见）

#### 5.4 历史记录

- 提供“我的任务历史”统一界面，分为“已发布”和“已接单”两个子视图，均可按状态、日期范围筛选。已完成任务可查看评价详情和交付物。
- 关于交付物附件，自任务进入“已完成”状态开始计时，保存30日，逾期清理，后果由任务发布者自行承担。

### 6、积分系统

用户初始拥有1000积分，不可充值，仅可通过接单、发布订单进行积分流转。

### 7、后台管理系统

后台管理系统仅供具备管理员权限的账户（可由校方指定1-3名教师或学生助理）登录，入口独立于用户前端。所有管理员操作均需留存操作日志（操作人、时间、IP、内容），日志永久保存，不可删除。

#### 7.1 仪表盘

- 展示平台核心运营数据，实时刷新：
  - 用户总数、今日新增用户、当前在线人数。
  - 任务总数、进行中任务数、待确认任务数、超时未交付任务数。
  - 待处理事项聚合卡片：待审核头像/昵称/公告栏数量、待处理申诉数量、被举报任务数量。
  - 平台收益账户当前积分余额、近7天提现手续费总额（积分）。
  - 近7天充值总额（元）与提现总额（元）对比折线图。

#### 7.2 用户管理

- 用户列表：展示所有用户（学号/工号、昵称、信用分、账户状态（正常/冻结）、注册时间、最后登录IP）。
- 搜索与筛选：支持按学号/昵称精确搜索，按账户状态、信用分区间（<40, 40-60, 60-80, 80-100）筛选。
- 账户状态管理：
  - 手动冻结/解冻账户（需填写理由，冻结期间限制发布/接单，但不影响进行中任务及积分支付）。
  - 处理毕业生离校账户：每月1日凌晨系统自动标记毕业账户为“待冻结”，管理员可在此处批量确认或单独延期（如研究生延期毕业）。每次延期最多30天，最多延期2次。
  - 信用分重置：对于信用分<40且重置机会尚未使用的用户，管理员在审核其500字陈述后可执行“恢复信用分至80分”操作，并记录操作理由。
- 操作日志：查看任一用户的所有关键操作记录（发布任务、接单、提现、修改资料等）。

#### 7.3 任务管理

- 任务列表：展示全平台所有任务（任务ID、名称、发布者、状态、报酬、发布时间、当前停留时长）。
- 强制干预：
  - 管理员可对违规任务（如涉黄、违法、校外商业广告）进行强制下架（状态变为“已取消”），系统划扣100%任务积分，并记录违规原因。
  - 对于处于“待确认”或“进行中”的争议任务，管理员可直接跳转至申诉处理流程。
- 分类迁移：当需要删除某个任务分类时，系统自动列出该分类下所有“发布中”和“进行中”的任务，管理员可批量将任务迁移至其他分类，迁移后才可删除原分类。

#### 7.4 审核管理

将原有散落的审核项集中展示，支持按类型筛选（头像/昵称/公告栏/举报/申诉）：

- 头像/昵称/公告栏审核：
  - 列表显示：申请人、申请类型（头像/昵称/公告栏）、申请内容（昵称原文或头像缩略图）、申请时间。
  - 操作按钮：【通过】【拒绝】。拒绝需填写简短原因（自动发送通知给用户）。
  - 超时标记：若申请超过24小时未处理，系统自动标记为“超时自动拒绝”，并记录到日志，通知管理员。
- 任务举报处理：
  - 展示被举报任务、举报人（可匿名）、举报理由、证据截图。
  - 管理员判定：核实（按文档规则划扣发布者100%任务积分，并根据情节决定冻结天数5天至永久）；驳回（举报无效，无惩罚）。
  - 处理完成后，系统自动通知举报人和发布者结果。
- 申诉处理（与任务争议相关）：
  - 展示申诉详情：任务ID、争议双方昵称、申诉方理由、被申诉方答辩（若有）、交付物及历史退回记录。
  - 管理员裁定：【判定完成】（积分划转给接单者，任务标记“已完成”）；【判定取消】（积分退还发布者，任务标记“已取消”）。
  - 裁定后需填写处理意见（≤200字），该意见将在任务详情页对双方可见。

#### 7.5 系统配置

管理员可动态调整以下参数，调整后立即生效，无需重启服务（满足非功能性需求6）：

- 信用分规则配置：
  - 完成率加分阈值（默认90%）、减分阈值（70%）；对应分值（默认+5/-5）可修改。
  - 好评率加分阈值（95%）、减分阈值（80%）；对应分值（默认+3/-3）可修改。
  - 超时扣分（默认-8）、主动放弃扣分（默认-3）、发布者无故下架扣分（默认-5）。
  - 信用分下限0分、上限100分；风险提示阈值（默认60）、接单限制阈值（默认40）。
- 超时与任务时限配置：
  - 无人接单自动取消天数（默认14天）。
  - 交付超时提醒提前量（默认24小时）。
  - 截止时间单次延长比例（默认50%）、最大延长次数（默认2次）。
- 附件与存储配置：
  - 任务附件大小上限（默认20MB）、交付物附件上限（30MB）。
  - 交付物附件保存天数（默认30天）。

#### 7.6 消息广播

- 管理员可编辑系统公告（标题+正文，最长1000字），选择发送范围（全部用户/仅发布者/仅接单者/指定学号列表）。公告将出现在用户消息通知列表的“系统通知”分类中，且可在前端以跑马灯或弹窗形式提醒（可配置是否强提醒）。
- 可预设定时广播（如每周日20:00 提醒用户评价未完成任务）。

### 8、通知系统

#### 8.1 通知类型与触发场景

| 类型     | 触发场景                         | 接收方       | 通知标题示例                             | 是否可跳转             |
| -------- | -------------------------------- | ------------ | ---------------------------------------- | ---------------------- |
| 任务通知 | 发布者选择中标者                 | 中标者       | 「您已中标：任务《XX》」                 | 跳转至任务详情         |
| 任务通知 | 发布者选择中标者                 | 落选者       | 「很遗憾，您未中标任务《XX》」           | 跳转至任务详情         |
| 任务通知 | 发布者主动下架任务               | 所有申请者   | 「任务《XX》已被发布者取消」             | 跳转至任务列表         |
| 任务通知 | 接单者超时未交付，发布者强制取消 | 接单者       | 「任务《XX》因超时被取消，信用分-8」     | 跳转至任务详情         |
| 任务通知 | 截止时间前24小时                 | 接单者       | 「【提醒】任务《XX》即将在24小时后截止」 | 跳转至交付页面         |
| 任务通知 | 发布者确认完成                   | 接单者       | 「任务《XX》已完成，报酬XX积分已到账」   | 跳转至评价入口         |
| 审核通知 | 头像/昵称/公告栏审核通过         | 申请人       | 「您的昵称修改申请已通过」               | 跳转至个人中心         |
| 审核通知 | 头像/昵称/公告栏审核拒绝         | 申请人       | 「您的头像审核未通过：原因：尺寸不符」   | 跳转至个人中心         |
| 系统通知 | 管理员发送公告                   | 指定范围用户 | 「【平台公告】五一假期提现暂停通知」     | 无跳转或跳转至公告详情 |
| 系统通知 | 信用分降至风险阈值以下           | 用户         | 「信用分低于60分，请及时改善行为」       | 跳转至信用分明细       |

#### 8.2 通知基本信息

- 发送方式：
  - 实时发送：任务状态变更、审核结果、充值提现结果等，在操作完成后立即插入通知记录。
  - 批量发送：系统公告由管理员触发后，后台异步插入所有目标用户的通知记录（若全站用户10万+，可分批插入）。
  - 定时提醒：每日凌晨扫描即将在24小时后截止的进行中任务，生成提醒通知。

#### 8.3 前端交互

- 顶部栏消息图标：
  - 显示未读数量红点（最大99+，超过显示“99+”）。
  - 点击弹出下拉面板（宽度360px），展示最近5条未读通知，底部有“查看更多”链接跳转至完整通知页面。
- 通知页面：
  - 支持按类型筛选，按时间倒序排列。
  - 每条通知右侧操作：【标记为已读】【删除】。支持多选批量操作。
  - 未读通知背景浅灰色，已读白色。
- 关联跳转：点击通知卡片（除系统公告外）自动标记为已读并跳转至 `target_url`。

#### 8.4 通知规则补充说明

- 同一任务对同一用户的重复通知（如多次退回修改）应合并或限制频率，避免刷屏。也即，同一任务同一用户，每30分钟最多发送1条同类型通知。
- 用户可（在个人中心-通知设置）关闭部分非关键通知类型（如“落选通知”），但任务中标、超时提醒、信用分变更等关键通知不可关闭。

## 四、布局要求

### 1、用户界面

1. **整体结构**：Web网页采用经典后台式布局，左侧为可收起的导航菜单，右侧为内容区。并且在内容区右上角存在固定的铃铛形状图标，代表通知界面的入口。
2. **导航菜单**：包含“任务大厅”、“发布任务”、“我的任务”、“个人中心”、“消息通知”等一级条目。其中“我的任务”下含发布/接单视角子菜单。
3. **任务大厅页面**：顶部为搜索栏和筛选排序区，主体为任务卡片网格或列表（默认网格视图，可切换列表），每张卡片显示任务关键信息。
4. **个人中心**：左侧标签切换基本信息、信用分明细、收支明细、评价记录；右侧展示对应内容。
5. **颜色与字体**：整体采用校徽色点缀的简洁浅色主题，字体使用系统默认无衬线字体，关键操作按钮颜色醒目，状态用标签色区分（进行中蓝色，已完成绿色，已取消灰色等）。
6. **响应式**：适配1920×1080及以上分辨率的桌面浏览器，移动端暂不支持，但需保证在平板横屏模式下基本可用。

### 2、管理员界面

1. 管理员界面则由“任务管理”、“申诉处理”、“申请处理”、“账户余额”等条目组成。分别管理任务状态、处理各类申诉、处理各类申请（头像、公告栏等）、系统账户的总余额等。

## 五、扩展需求

1. **任务分类管理**
   1. 管理员在后台“分类管理”界面可对任务分类进行增删改查。
   2. 分类字段：分类名称（唯一，最长10字）、排序权重、启用状态。删除分类前需确保该分类下无进行中任务，否则禁止删除，提示先迁移。
2. **热门任务推荐**
   1. 接单者视角的首页“推荐任务”模块，根据用户最近完成的10个任务中的分类频次，推荐同类未接单且即将截止的任务。若用户无历史，则推荐热门任务（最近7天申请人数最多的前20条）。
   2. 推荐算法每天凌晨重新计算一次，并更新排序。

## 六、非功能性需求

1. **项目形态**：用户端与管理员端均采用B/S架构的Web网页端，前后端分离，前端使用Vue/React，后端Spring Boot等，数据库MySQL/PostgreSQL。
2. **性能指标**：
   1. 任务列表首页加载（含推荐）响应时间 < 800ms（P95）。
   2. 搜索与筛选结果返回 < 500ms。
   3. 状态变更、申请接单等写操作响应 < 200ms。
3. **安全**：
   1. OAuth token安全存储 。
   2. 严格用户数据隔离，接口做权限校验，普通用户不可越权访问他人私有数据（如待审核申请、私信等）。
   3. 防范XSS/CSRF/SQL注入，所有用户输入做过滤转义。
4. **易用性需求**：界面简洁清晰，核心流程步骤在3步内完成；关键操作提供二次确认；表单增加实时校验与错误提示，符合大学生使用习惯。
5. **可靠性需求**：
   1. 所有操作有日志流水，可追踪审计。
6. **可维护性**：信用分计算规则、超时天数、积分上下限等业务参数通过配置中心管理，支持动态刷新，无需重启服务。

## 七、附录

1. 本文档中的“字”不区分中文、英文、数字、符号。每一个中文，每一个英文，每一个数字，每个符号，都算一个“字”。
2. 关于账户的冻结：也即账户无法发布新的订单，接受新的订单。但已接受、发布的订单以及积分系统维持正常（也即可以支付、充值以完成未完成的交易）。
3. 占位
4. 对于涉及到比率计算得到的最终数字，均向上取整。
5. 关于信誉分：满分100分，最低0分，扣到0分之后不再扣除。
6. 所有的自动拒绝，均通知用户与管理员。并且，管理员恢复申请之后，此申请直接进入审核列表，并重新计时24小时。
7. 所有的过滤机制，比如检测任务发布中是否有敏感词，对话中是否存在敏感词等，如果在24小时内累计出现三次被过滤的情况，则扣除3分信用分，并向用户发送友好交流提醒。
8. 关于任务的优先级。除扩展需求外的所有需求，优先级相同且必须完成。扩展需求如条件不允许可简单完成/不完成。